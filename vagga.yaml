containers:
  jekyll:
    setup:
      - !Alpine v3.4
      - !BuildDeps
        - build-base
        - libffi-dev
      - !GemInstall
        - json
        - jekyll

commands:
  jekyll: !Command
    description: run jekyll command
    container: jekyll
    run: [jekyll]
